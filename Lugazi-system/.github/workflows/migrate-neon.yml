name: Migrate Neon Database Schema

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'Lugazi-system/lib/db/src/schema/**'
      - 'Lugazi-system/lib/db/drizzle.config.ts'
      - 'Lugazi-system/pnpm-workspace.yaml'
      - 'pnpm-lock.yaml'

jobs:
  migrate:
    name: Push Schema to Neon
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Install dependencies
        run: cd Lugazi-system && pnpm install --no-frozen-lockfile --ignore-scripts

      - name: Push schema to Neon
        run: cd Lugazi-system && pnpm --filter @workspace/db run push
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
